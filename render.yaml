services:
  - type: web
    name: dj-booking-app-kai99
    env: docker
    plan: free
    branch: main
    dockerfilePath: Dockerfile
    startCommand: "node server.js"
    envVars:
      - key: NODE_ENV
        value: production
      - key: FRONTEND_URL
        value: https://dj-booking-app-kai99.onrender.com
      - key: JWT_SECRET
        sync: false
      - key: DATABASE_URL
        fromDatabase:
          name: dj-booking-db-kai99
          property: connectionString
      - key: ADMIN_PASSWORD
        value: admin123
      - key: EMAIL_HOST
        value: smtp.gmail.com
      - key: EMAIL_PORT
        value: '587'
      - key: EMAIL_SECURE
        value: 'false'
      - key: EMAIL_USER
        value: allfriendsavhire@gmail.com
      - key: EMAIL_PASSWORD
        value: brxqelyyjrejtqgt

databases:
  - name: dj-booking-db-kai99
    plan: free
    databaseName: dj_booking_app
    user: dj_booking_app_user

# Note: You still need to connect this repository to Render (via GitHub or GitLab)
# and create the service from the Render dashboard, or use the Render API.